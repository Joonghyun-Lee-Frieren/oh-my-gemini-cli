# oh-my-gemini-cli (OmG)

<p align="center">
  <strong>Gemini CLI のためのコンテキストエンジニアリング型マルチエージェントハーネス</strong>
</p>

<h3 align="center">
  <em>Gemini thinks. OmG orchestrates.</em>
</h3>

<p align="center">
  <a href="https://www.npmjs.com/package/oh-my-gemini-cli"><img src="https://img.shields.io/npm/v/oh-my-gemini-cli.svg" alt="npm version"></a>
  <a href="https://github.com/Joonghyun-Lee-Frieren/oh-my-gemini-cli/blob/main/LICENSE"><img src="https://img.shields.io/badge/license-MIT-blue.svg" alt="License: MIT"></a>
  <a href="https://nodejs.org/"><img src="https://img.shields.io/badge/node-%3E%3D20-green.svg" alt="Node.js"></a>
</p>

<p align="center">
  <a href="https://joonghyun-lee-frieren.github.io/oh-my-gemini-cli/">Landing Page</a>
</p>

<p align="center">
  <a href="../README.md">English</a> | <a href="./README_ko.md">한국어</a> | <strong>日本語</strong> | <a href="./README_zh.md">中文</a> | <a href="./README_es.md">Español</a> | <a href="./README_pt.md">Português</a> | <a href="./README_fr.md">Français</a> | <a href="./README_nl.md">Nederlands</a> | <a href="./README_pl.md">Polski</a> | <a href="./README_uk.md">Українська</a> | <a href="./README_ru.md">Русский</a> | <a href="./README_de.md">Deutsch</a> | <a href="./history.md">Changelog</a>
</p>





> "Claude Code's core competitiveness isn't the Opus or Sonnet engine. It's Claude Code itself. Surprisingly, Gemini works well too when attached to Claude Code."
>
> — **Jeongkyu Shin (CEO of Lablup Inc.)**, *from a YouTube Channel interview*

*このプロジェクトはこの洞察から始まりました: 「そのハーネスを Gemini CLI に適用したらどうなるだろう？」*





> **[Gemini CLI](https://github.com/google-gemini/gemini-cli)** を単一セッションのツールから
> **コンテキストエンジニアリング型マルチエージェントオーケストレーションシステム**へ拡張します。





## 目次

- [なぜ oh-my-gemini-cli か？](#なぜ-oh-my-gemini-cli-か)
- [クイックスタート](#クイックスタート)
- [コア概念: コンテキストエンジニアリング](#コア概念-コンテキストエンジニアリング)
- [マルチエージェントシステム](#マルチエージェントシステム)
- [ASCII ダッシュボード](#ascii-ダッシュボード)
- [モデルオーケストレーション](#モデルオーケストレーション)
- [エージェントカタログ](#エージェントカタログ)
- [スキルカタログ](#スキルカタログ)
- [カスタムコマンド](#カスタムコマンド)
- [Telegram / Discord ボット](#telegram--discord-ボット)
- [設定](#設定)
- [CLI コマンド](#cli-コマンド)
- [プロジェクト構造](#プロジェクト構造)
- [コントリビューション](#コントリビューション)
- [ライセンス](#ライセンス)

---

## なぜ oh-my-gemini-cli か？

Gemini CLI は強力です: 1M トークンのコンテキストウィンドウ、Google Search グラウンディング、MCP サポート。  
しかし大規模な作業には構造が必要です。

OmG が追加する機能:

- **マルチエージェントオーケストレーション**: タスクタイプに応じて複数のエージェントを並列で起動し、連携させます
- **コンテキストエンジニアリング**: プロンプトキャッシュ最適化でレイテンシとコストを削減します
- **リアルタイム ASCII ダッシュボード**: リッチなターミナル UI で全エージェントを監視します
- **デュアルモデル戦略**: Gemini Pro（計画）+ Gemini Flash（実行）
- **外部 LLM サポート**: OAuth/API で Claude、GPT、その他のモデルに接続できます
- **リモート制御**: Telegram/Discord ボットでセッションを監視・制御できます

OmG はフォークではなくアドオンです。Gemini CLI のネイティブ拡張ポイント（MCP、カスタムコマンド、`GEMINI.md`）のみを使用します。

---

## クイックスタート

### 要件

- macOS、Linux、または Windows（WSL2 推奨）
- Node.js >= 20
- [Gemini CLI](https://github.com/google-gemini/gemini-cli) がインストール済み
- Gemini 認証が設定済み（Google ログイン、API キー、または Vertex AI）

### インストール

```bash
# 推奨: npm レジストリパッケージが利用できない場合でも動作します
npm install -g github:Joonghyun-Lee-Frieren/oh-my-gemini-cli#main

# オプション: 環境で npm レジストリパッケージが利用可能な場合
# npm install -g oh-my-gemini-cli

omg setup
omg doctor
```

> 注意: `npm install -g oh-my-gemini-cli` が `404 Not Found` で失敗する場合は、上記の GitHub インストールコマンドを使用してください。

### npm 公開切り替えチェックリスト

- npm パッケージページと `latest` dist-tag が利用可能であることを確認
- クリーンな環境で `npm install -g oh-my-gemini-cli` が動作することを検証
- ドキュメントのデフォルトインストールコマンドを GitHub から npm レジストリに切り替え
- トラブルシューティングセクションで GitHub インストールをフォールバックとして残す

### LLM 自動インストール

以下の内容を任意の AI コーディングエージェント（Cursor、Claude Code、Gemini CLI など）に貼り付けてください:

```
Install and configure oh-my-gemini-cli:
https://raw.githubusercontent.com/Joonghyun-Lee-Frieren/oh-my-gemini-cli/main/docs/guide/installation.md
```

### 初回セッション

```bash
# 基本起動（Gemini CLI + ダッシュボード）
omg

# マルチエージェントチームモード
omg team "Implement OAuth authentication"

# 特定のエージェントで作業
omg --agent architect "Analyze this codebase architecture"
```

---

## コア概念: コンテキストエンジニアリング

> **"Cache Rules Everything Around Me"**
> — エージェント時代においても、キャッシュは依然として支配的です。

oh-my-gemini-cli のコア哲学は **コンテキストエンジニアリング** です。  
[Claude Code プロンプトキャッシングの教訓](https://x.com/trq212/status/2024574133011673516) に触発され、Gemini CLI に適応しました。

### プロンプトキャッシングとは？

長時間実行されるエージェントワークフローでは、**プロンプトキャッシング** が以前の計算を再利用し、レイテンシとコストを劇的に削減します。  
キャッシングは **プレフィックスマッチング** に基づきます。

```
Request structure (cache-optimized order):

┌──────────────────────────────────┐  ← global cache (all sessions)
│  Static system prompt            │
│  Tool definitions (including MCP)│
├──────────────────────────────────┤  ← project cache
│  GEMINI.md (project context)     │
├──────────────────────────────────┤  ← session cache
│  Session context                 │
├──────────────────────────────────┤  ← changes every turn
│  Conversation messages           │
└──────────────────────────────────┘
```

### 5つの核心原則

#### 1. プレフィックス安定性

プレフィックスの変更は、その後のキャッシュを無効化します。  
OmG は静的コンテンツを先に、動的コンテンツを後に配置してキャッシュヒットを最大化します。

#### 2. ツールセット不変性

ツールの追加/削除はキャッシュを破壊します。OmG は以下でこれを回避します:
- 計画モードではツールリストを変更する代わりに状態遷移ツールを使用
- セッション中にツールセットを変更する代わりに、遅延読み込み MCP スタブを使用

#### 3. モデル切り替えよりサブエージェント

プロンプトキャッシュはモデル固有です。  
1つのスレッドでモデルを切り替える代わりに、OmG は専用モデルキャッシュを持つサブエージェントを使用します。

#### 4. キャッシュセーフ圧縮

コンテキストを圧縮する必要がある場合、OmG はキャッシュセーフフォークで親キャッシュプレフィックスを保持します。

#### 5. 更新のためのシステムリマインダー

システムプロンプトの内容を直接変更する代わりに、OmG はメッセージフローに更新シグナルを追加します。

### キャッシュ監視

OmG はキャッシュヒット率を稼働率のように扱います。

```bash
omg status --cache
```

---

## マルチエージェントシステム

OmG はタスクタイプに基づいて複数のエージェントを並列で作成し、実行を調整します。

### アーキテクチャ

```
User task request
    │
    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────────┐
│ TaskRouter   │───▶│ TaskQueue    │───▶│ AgentPool        │
│ classification│   │ priority     │    │ (max 6 parallel) │
└──────────────┘    └──────────────┘    └─────────┬────────┘
                                                  │
                    ┌─────────────────────────────┤
                    │              │               │
                    ▼              ▼               ▼
              ┌──────────┐  ┌──────────┐  ┌──────────────┐
              │ Architect │  │ Executor │  │ Executor #2  │
              │ (Pro)     │  │ #1 Flash │  │ (Flash)      │
              └─────┬─────┘  └────┬─────┘  └──────┬───────┘
                    │              │               │
                    ▼              ▼               ▼
              ┌──────────────────────────────────────────┐
              │              EventBus                     │
              ├──────────┬──────────────┬────────────────┤
              ▼          ▼              ▼                ▼
          Dashboard   omg_state     Telegram         Discord
          (ASCII TUI)  (MCP)          Bot              Bot
```

### エージェントタイプ

| Agent | Model | Role |
|------|------|------|
| **Architect** | Gemini Pro | アーキテクチャ分析、設計判断、依存関係マッピング |
| **Planner** | Gemini Pro | タスク分解、実行計画、優先順位付け |
| **Executor** | Gemini Flash | コード生成、ファイル編集、リファクタリング（並列 N） |
| **Reviewer** | Gemini Pro | コードレビュー、品質チェック、セキュリティチェック |
| **Debugger** | Gemini Pro | エラー分析、デバッグ、失敗したテストの修正 |
| **Researcher** | Gemini Pro | Web リサーチ、ドキュメント分析、ライブラリ比較 |
| **Quick** | Gemini Flash | 小規模タスク: タイポ修正、フォーマット、小さな変更 |

### ライフサイクル

```
idle ──▶ assigned ──▶ running ──▶ completed
                         │            │
                         │            └──▶ (return to agent pool)
                         │
                         └──▶ failed ──▶ retry (max 3)
                                           └──▶ escalate
```

### パイプライン例

```bash
omg team "Implement OAuth authentication"
omg team --workers 4 "Refactor entire API layer"
omg --agent executor "Convert this function to TypeScript"
omg team status
omg team shutdown
```

---

## ASCII ダッシュボード

Ink（CLI 用 React）で構築されたリアルタイムターミナルダッシュボード。

### ダッシュボードプレビュー

```
+----------------------------------------------------------------------+
| OmG  oh-my-gemini-cli   TIME 03:42   PARTY [####--] 4/6              |
+--------------------------------+-------------------------------------+
| PARTY MEMBERS                  | QUEST LOG                           |
| Active: 4 / Total: 6           | 2/7 quests cleared                  |
| Planner   [gemini-pro]   ATK   | [DONE ] analyze-auth   -> Plan      |
| Architect [gemini-pro]   ATK   | [RUN  ] impl-oauth    -> Exec#1     |
| Exec#1    [gemini-flash] ATK   | [RUN  ] impl-token    -> Exec#2     |
| Reviewer  [gemini-pro]   IDLE  | [WAIT ] code-review   -> Review     |
+--------------------------------+-------------------------------------+
| CACHE [#####-] 94% | TOKENS 12.4k/1M | COST -$0.03                   |
+----------------------------------------------------------------------+
```

### ショートカット

| Key | Action |
|----|------|
| `q` / `Ctrl+C` | 終了 |
| `p` | 全エージェント一時停止 |
| `r` | 再開 |
| `d` | 選択したエージェントの詳細ストリームを表示 |
| `t` | Telegram/Discord 同期の切り替え |
| `Tab` | エージェント間でフォーカス移動 |
| `Enter` | フォーカス中のエージェント詳細パネルを開く |
| `1`-`9` | 番号でエージェントにフォーカス |

---

## モデルオーケストレーション

### デュアルモデル戦略

OmG はデフォルトで **計画は Pro、実行は Flash** を使用します。

### 外部 LLM 統合

```bash
# OAuth 経由で Claude
omg config set external.claude.oauth true
omg auth claude

# API キーで OpenAI
omg config set external.openai.api_key "sk-..."
```

---

## エージェントカタログ

Gemini CLI 内で `/prompts:name` で使用します。

| Agent | Command | Description |
|------|------|------|
| Architect | `/prompts:architect` | アーキテクチャ分析、設計判断、依存関係グラフ |
| Planner | `/prompts:planner` | タスク分解、実行計画、マイルストーン |
| Executor | `/prompts:executor` | コード生成と編集、リファクタリング |
| Reviewer | `/prompts:reviewer` | レビュー: セキュリティ/パフォーマンス/品質 |
| Debugger | `/prompts:debugger` | エラー分析、スタックトレース解釈、修正 |
| Researcher | `/prompts:researcher` | Web 検索、ドキュメント分析、ライブラリ比較 |
| Quick | `/prompts:quick` | タイポ修正、フォーマット、小さな変更 |

---

## スキルカタログ

Gemini CLI 内で `$skill-name` で使用します。

| Skill | Trigger | Description |
|------|------|------|
| plan | `$plan` | Pro ベースの戦略的計画 |
| execute | `$execute` | Flash ベースの高速実装 |
| team | `$team` | マルチエージェントチームオーケストレーション |
| research | `$research` | 並列リサーチとドキュメント分析 |
| context-optimize | `$context-optimize` | コンテキストウィンドウ最適化 |

---

## カスタムコマンド

TOML カスタムコマンドをサポートします。

**配置場所**
- グローバル: `~/.gemini/commands/*.toml`
- プロジェクト: `.gemini/commands/*.toml`

---

## Telegram / Discord ボット

OmG セッションをリモートで監視・制御できます。

```bash
omg config set bot.telegram.token "YOUR_BOT_TOKEN"
omg bot telegram start

omg config set bot.discord.token "YOUR_BOT_TOKEN"
omg bot discord start
```

---

## 設定

### ファイルの配置場所

- グローバル: `~/.gemini/omg-settings.json`
- プロジェクト: `.gemini/omg-settings.json`（優先）

### 例

```json
{
  "agents": {
    "max_concurrent": 6,
    "default_planner": "gemini-3.1-pro",
    "default_executor": "gemini-3.1-flash"
  },
  "context": {
    "cache_monitoring": true,
    "cache_target_rate": 0.9,
    "compaction_strategy": "cache-safe",
    "prefix_stability": true
  }
}
```

---

## CLI コマンド

```bash
omg                         # Gemini CLI + ダッシュボードを起動
omg setup                   # プロンプト/スキル/コマンド/MCP/GEMINI.md をインストール
omg doctor                  # 診断を実行
omg update                  # 最新版に更新
omg team <description>      # マルチエージェントチームモード
omg team status             # チームステータス
omg team shutdown           # チームを停止
omg status                  # エージェント/キャッシュステータス
omg status --cache          # キャッシュヒット詳細
omg status --agents --json  # JSON でエージェントステータス
omg status --tasks --json   # JSON でタスクパイプライン
omg status --context --json # JSON でコンテキスト使用量
omg config set <key> <val>  # 設定をセット
omg config get <key>        # 設定を取得
omg bot telegram start      # Telegram ボットを起動
omg bot discord start       # Discord ボットを起動
omg help                    # ヘルプを表示
```

### ランタイムフラグ

```bash
--agent <type>     # 特定のエージェント（architect, executor, ...）
--workers <N>      # チームワーカー数（デフォルト: 4）
--model <model>    # モデルオーバーライド
--no-dashboard     # ダッシュボードを無効化
--verbose          # 詳細ログ
--dry-run          # 計画のみ、実行なし
```

### ステータス JSON フィールド

`omg status --json` の戻り値:

- `agents`: アクティブ/合計とエージェントリスト
- `tasks`: 完了/実行中/キュー/失敗のカウンターとタスクリスト
- `cache`: `hit_rate`、`hits`、`misses`、ターゲットレート
- `cache_history`: 最近のキャッシュスナップショット
- `context`: `used`、`limit`、`percentage`、圧縮しきい値

---

## プロジェクト構造

```
oh-my-gemini-cli/
├── bin/omg.js                        # CLI エントリーポイント
├── src/
│   ├── cli/                          # CLI コマンド
│   ├── agents/                       # マルチエージェントシステム
│   ├── dashboard/                    # ASCII ダッシュボード TUI
│   ├── context/                      # コンテキストエンジニアリングエンジン
│   ├── orchestrator/                 # モデルオーケストレーション
│   ├── mcp/                          # MCP サーバー
│   ├── bot/                          # Telegram/Discord ボット
│   └── shared/                       # 共有ユーティリティ
├── prompts/                          # エージェントプロンプト
├── skills/                           # ワークフロースキル
├── commands/                         # カスタムコマンド（TOML）
├── templates/                        # GEMINI.md テンプレート
├── docs/
│   ├── README_ko.md                  # 韓国語ドキュメント
│   ├── history.md                    # 変更履歴（英語）
│   ├── history_ko.md                 # 変更履歴（韓国語）
│   └── guide/
│       ├── installation.md           # インストールガイド
│       └── context-engineering.md    # コンテキストエンジニアリングガイド
├── package.json
├── tsconfig.json
├── README.md                         # 英語ドキュメント（このファイル）
└── LICENSE
```

---

## `omg setup` の動作

| Step | Description |
|------|------|
| 1 | `.omg/` ランタイムディレクトリを作成（状態、計画、ログ） |
| 2 | プロンプトを `~/.gemini/prompts/` または `.gemini/prompts/` にインストール |
| 3 | ワークフロースキルをインストール |
| 4 | カスタムコマンドを `~/.gemini/commands/` にインストール |
| 5 | `settings.json` に MCP サーバーを登録（`omg_state`、`omg_memory`、`omg_context`、`omg_orchestrator`） |
| 6 | プロジェクト `GEMINI.md` を生成 |
| 7 | デフォルト `omg-settings.json` を生成 |

---

## テックスタック

- **ランタイム**: Node.js >= 20
- **言語**: TypeScript
- **TUI**: Ink 5（CLI 用 React）
- **MCP**: `@modelcontextprotocol/sdk`
- **Telegram**: `telegraf`
- **Discord**: `discord.js`
- **ビルド**: esbuild
- **テスト**: Vitest

---

## インスピレーション

- [Gemini CLI](https://github.com/google-gemini/gemini-cli) - Google のオープンソース AI ターミナルエージェント
- [oh-my-claudecode](https://github.com/Yeachan-Heo/oh-my-claudecode) - Claude Code 向けチームファーストマルチエージェントオーケストレーション
- [oh-my-codex](https://github.com/Yeachan-Heo/oh-my-codex) - Codex CLI ハーネス
- [oh-my-opencode](https://github.com/code-yeongyu/oh-my-opencode) - OpenCode ハーネス
- [Claude Code Prompt Caching](https://x.com/trq212/status/2024574133011673516) - コンテキストエンジニアリングの原則

---

## コントリビューション

コントリビューションを歓迎します。

- バグレポートと機能提案
- 新しいエージェントプロンプト
- 新しいスキルとカスタムコマンド
- ドキュメントの改善
- Telegram/Discord ボットの改善

## ライセンス

[MIT](../LICENSE)

## 連絡先

📧 kissdesty@gmail.com
