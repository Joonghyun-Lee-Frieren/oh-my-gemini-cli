# /omg:status — 에이전트 및 시스템 상태 조회
# 현재 활성 에이전트, 작업 파이프라인, 캐시 적중률을 한눈에 확인합니다.

[command]
name = "omg:status"
description = "OMG 에이전트 상태 및 시스템 현황을 조회합니다"

[command.args]
detail = { type = "boolean", default = false, description = "상세 정보 포함 여부" }

[prompt]
text = """
현재 oh-my-gemini-cli 시스템의 상태를 종합적으로 확인하고 보고해주세요.

## 확인 항목

### 1. 에이전트 상태
!{omg status --agents --json}

활성 에이전트 목록과 각 에이전트의 현재 작업, 진행률을 요약해주세요.

### 2. 작업 파이프라인
!{omg status --tasks --json}

현재 작업 큐의 상태를 요약해주세요:
- 완료된 작업, 진행 중인 작업, 대기 중인 작업 수
- 실패한 작업이 있다면 원인 요약

### 3. 캐시 적중률
!{omg status --cache --json}

캐시 상태를 확인하고 목표 적중률(90%) 대비 현재 상태를 보고해주세요.
적중률이 낮다면 원인과 개선 방안을 제시해주세요.

### 4. 리소스 사용량
- 컨텍스트 윈도우 사용률
- 세션 시작 이후 사용된 토큰 수
- 예상 비용

## 출력 형식

간결한 상태 보고서 형식으로 출력해주세요:

```
📊 OMG Status Report
━━━━━━━━━━━━━━━━━━━━
🤖 Agents: {active}/{total} active
📋 Tasks:  {done}/{total} done, {running} running, {queued} queued
💾 Cache:  {hit_rate}% hit rate (target: >90%)
📦 Tokens: {used}/{limit} ({percentage}%)
💰 Cost:   ${cost} (session)
⏱️ Uptime: {duration}
```

문제가 발견되면 ⚠️ 경고와 함께 권장 조치를 제시해주세요.
"""
